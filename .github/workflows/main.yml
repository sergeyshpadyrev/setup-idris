on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run
        uses: ./ 
      - name: Check user
        run: whoami
      - name: Check idris
        run: /root/.idris2/bin/idris2 -v
