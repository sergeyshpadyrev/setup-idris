name: 'Setup Idris'
description: 'Install Idris compiler'
inputs:
  idris-version: 
    description: 'Idris version'
    required: true
    default: 'idris2'
runs:
  using: 'docker'
  image: ${{ github.inputs.idris-version }}
